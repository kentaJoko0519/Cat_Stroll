<div class="container">
  <div class="row">
    <div class="col-md-12  text-center mt-5">
      <h3><strong>新規登録</strong></h3>
    </div>

    <div class="col-md-8 mt-4" style="margin-left:200px;">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "public/shared/error_messages", resource: resource %>
      <table class="table table-borderless">
        <tr>
          <td>名前</td>
          <td>
            (姓)<%= f.text_field :first_name %>
            (名)<%= f.text_field :last_name %>
          </td>
        </tr>
        <tr>
          <td>フリガナ</td>
          <td>
            (姓)<%= f.text_field :first_name_kana %>
            (名)<%= f.text_field :last_name_kana %>
          </td>
        </tr>
        <tr>
          <td>メールアドレス</td>
          <td>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", style: "width:90%" %>
          </td>
        </tr>
        <tr>
          <td>パスワード(６文字以上)</td>
          <td><%= f.password_field :password, autocomplete: "new-password", style: "width:60%" %></td>
        </tr>
        <tr>
          <td>パスワード(確認用)</td>
          <td><%= f.password_field :password_confirmation, autocomplete: "new-password", style: "width:60%" %></td>
        </tr>
      </table>

        <div class="col-md-12 text-center">
          <%= f.submit "新規登録", style: "width:20%", class: "btn btn-success" %>
        </div>
    <% end %>
    </div>
  </div>

  <div class="row text-center">
    <div class="col-md-12 mt-5" style="margin-left:10px;">
      <p>
        既に新規登録されている方は、<br>
        <%= link_to"こちら" ,new_user_session_path %>からログインしてください。
      </p>
    </div>
  </div>

</div>



